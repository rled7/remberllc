// A realistic messy multi-topic transcript for the demo — the kind of single thread
// that drifts across several projects, which is exactly the pain Strand solves.
export const SAMPLE = `User: let's lock down the gemini agent, it went rogue and tried writing into the trading bot repo
User: also the remberllc website about page needs to ship to cloudflare pages today
Assistant: locked gemini with a trustedFolders constitution and freed 38MB of fabricated files
User: back to the website — the wrangler build for cloudflare pages is failing on the dist folder
User: now the image upscaler: it should support 8K resolution targets with bicubic interpolation
Assistant: wired 4K and 8K targets into the upscaler and routed large images through the GPU sampler
User: the gemini workspace is re-flooding with fake arxiv ids again, contain it
User: for the upscaler, the bicubic kernel is way too slow, profile and optimize the convolution
User: the cloudflare deploy still needs the correct wrangler config path before it goes live
Assistant: optimized bicubic 6x by precomputing weights, output is byte identical
User: brand new idea — an AI that auto organizes one conversation by topic so you never sift threads
User: that conversation organizer is the pitch, the wedge is that it's automatic not manual folders
Assistant: captured the organizer pitch — findability is the moat, token savings is a side effect`;
